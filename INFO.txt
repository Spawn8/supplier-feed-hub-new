How to save:

Stage them:

git add .

---
Commit them:

git commit -m "feat: improve sidebar UI + fix login page"

---
Push them:

git push origin main

#or If this is a milestone → add a manual tag:

git tag -a v0.2.0 -m "Supplier modal + workspace switching ready"
+
git push origin v0.2.0